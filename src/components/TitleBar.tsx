import React from "react";
interface TitleBarProps {
  title: string;
  description?: string;
}
export function TitleBar({
  title,
  description
}: TitleBarProps) {
  return <div className="border-b">
      
    </div>;
}